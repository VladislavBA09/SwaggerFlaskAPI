tags:
  - name: DeleteGroup

produces:
  - application/json
  - application/xml

parameters:
  - name: group_id
    in: path
    description: 'group_id'
    required: true
    example:
      info: 'Delete group from database'
    schema:
      type: string
      properties:
        name:
          type: string

responses:
    '200':
      description: Successful response
    '400':
      description: Bad Request
    '500':
      description: Internal Server Error